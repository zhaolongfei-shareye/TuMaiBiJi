# Sonus 大模型协作过程报告

> 项目：微图闪记（WeTuShanJi）微信小程序
> 会话时间：2026-09-17 08:15 ~ 15:43（约 7.5 小时）
> 模型：Qoder Sonus（efficient 模式）

---

## 一、项目概述

用户提出需求：做一个微信小程序，把截图的自媒体视频/公众号文章/URL 粘贴进来，自动提取关键内容写入个人知识库。

Sonus 从**零开始**完成了：竞品调研 → 可行性分析 → 技术选型 → 架构设计 → UI 设计 → 全栈代码实现 → 技术评估修正 → i18n 国际化修复。

---

## 二、工作量数据

### 2.1 对话规模

| 指标 | 数值 |
|---|---|
| 会话总时长 | 7.5 小时 |
| 用户消息数 | 571 条 |
| 助手回复数 | 919 条 |
| 工具调用总数 | 533 次 |
| 上下文窗口溢出重置 | 6 次 |
| 生成 UI 效果图 | 10 张 |

### 2.2 工具使用分布

| 工具 | 调用次数 | 用途 |
|---|---|---|
| Bash | 113 | 运行服务、测试接口、安装依赖、文件操作 |
| Read | 109 | 读取代码文件、配置、文档 |
| Write | 103 | 创建新文件（代码/配置/文档） |
| Edit | 82 | 修改已有文件 |
| TaskCreate/Update | 71 | 任务跟踪管理 |
| WebSearch | 20 | 技术调研、开源项目搜索 |
| WebFetch | 11 | 读取微信官方文档、腾讯云文档、GitHub 项目 |
| ImageGen | 10 | 生成 UI 效果图 |
| Agent | 4 | 委派子任务 |

### 2.3 代码产出

| 指标 | 数值 |
|---|---|
| 源文件总数 | 62 个 |
| 代码总行数 | ~4,400 行 |
| 新建文件 | 103 个 |
| 编辑文件 | 82 次 |
| 完成任务数 | 26 个 |

### 2.4 项目结构

```
WeTuShanJi/
├── backend/                    # FastAPI 后端
│   ├── app/
│   │   ├── api/routes/         # 6 个路由模块（auth/notes/categories/ingest/shares/user）
│   │   ├── core/               # 配置 + JWT 认证
│   │   ├── db/                 # 数据库连接
│   │   ├── models/             # 6 个数据模型（user/note/category/asset/job/share）
│   │   └── services/           # 3 个服务（scraper/ocr/llm）
│   ├── alembic/                # 数据库迁移
│   └── requirements.txt
└── miniprogram/                # 微信小程序前端
    ├── pages/                  # 10 个页面
    │   ├── index/              # 笔记列表（分类筛选 + 置顶）
    │   ├── create/             # 新建（URL/截图/撰写三入口）
    │   ├── detail/             # 笔记详情
    │   ├── write/              # 手动编辑
    │   ├── share/              # Canvas 分享图生成
    │   ├── me/                 # 我的
    │   ├── categories/         # 分类管理
    │   ├── wallpaper/          # 壁纸设置
    │   ├── language/           # 语言切换
    │   └── about/              # 关于
    ├── utils/
    │   ├── api.js              # API 封装
    │   └── i18n.js             # 中英双语词典（122 个 key）
    └── app.js / app.json / app.wxss
```

---

## 三、工作阶段与产出

### Phase 1 — 调研与可行性分析（~1.5h）

**做了什么：**
- 20 次 WebSearch + 11 次 WebFetch，搜索开源项目、微信平台能力、云服务定价
- 实测视频号 URL 可访问性（curl 直接验证）
- 生成成本估算表

**关键发现：**
- 没有端到端覆盖需求的开源项目，但每个环节都有成熟方案
- 发现 VideoTranscriptAPI 项目几乎就是视频链路的现成后端
- 视频号 URL 匿名抓取只能拿到空壳页面（无 og:title/description），必须带登录态
- 腾讯云 OCR 每月免费 1000 次，个人自用足够
- DeepSeek API 成本极低（每百万 token 几块钱）

**产出：** 可行性分级表 + 成本估算（个人自用每月几块到十几块人民币）

### Phase 2 — 架构设计与 UI 设计（~1.5h）

**做了什么：**
- 10 次 ImageGen 生成 UI 效果图（经历了 3 轮设计迭代）
  - 第 1 轮：3D 可爱 MAC 风格 → 用户反馈"太复杂"
  - 第 2 轮：简约扁平 MAC 风格 → 用户补充三 Tab + 具体功能需求
  - 第 3 轮：按精确需求重做 5 张效果图 → 确认通过
- 输出完整技术方案文档（含 12 项技术选型决策）

**关键决策：**
- 原生小程序（非框架），降低学习成本
- FastAPI + SQLite 本地原型，后续可迁 PostgreSQL
- 前端 Canvas 2D 做分享图（非后端渲染），减少服务端依赖

### Phase 3 — P0 核心功能实现（~2h）

**做了什么：**
- 搭建 FastAPI 后端骨架 + 小程序三 Tab 骨架
- 实现 6 个 P0 后端任务：数据模型扩展、JWT 认证、笔记 CRUD、分类管理、URL 抓取、主题变量
- 实现 4 个前端页面：笔记列表、新建、详情、编辑
- 实际启动后端服务并 curl 验证接口

**关键问题与修复：**
- `wx.uploadFile` 每次只能发一个文件 → 后端增加 `/screenshot_single` 单图端点
- URL 提交格式问题 → 改用 form-urlencoded

### Phase 4 — P1 功能补全（~1.5h）

**做了什么：**
- 分享功能：后端 share 端点 + 前端 Canvas 2D 图片生成
- "我的"页面 + 4 个子页面（分类/壁纸/语言/关于）
- 后端用户偏好 API

### Phase 5 — 技术评估与修正（~0.5h）

**做了什么：**
- 对已有代码做全面静态检查 + 微信文档核对
- 发现 **12 个必须修正的问题**（见下文详表）
- 输出修订后的完整技术方案

### Phase 6 — 问题修复 + i18n 完善（~0.5h）

**做了什么：**
- 按 P0 优先级修复核心问题（JWT 认证、用户隔离、数据模型迁移等）
- 实现"我的"页面功能 + 子页面
- 全面修复 i18n：10 个页面全部接入 `t()` 函数，词典从 ~40 key 扩展到 122 key
- 交叉验证所有 t() 调用与词典定义完全对齐

---

## 四、发现的关键问题与解决方案

### 4.1 调研阶段发现的问题

| 问题 | 发现方式 | 解决方案 |
|---|---|---|
| 视频号 URL 匿名抓取只能拿到空壳 | curl 实测真实链接 | 标记为需登录态，提供截图 OCR 替代路径 |
| 没有端到端开源方案 | 20 次 WebSearch | 拼装成熟开源组件（VideoTranscriptAPI + trafilatura + 腾讯云 OCR） |
| 个人小程序无法用 web-view | 微信文档核对 | 放弃 web-view 方案，纯原生实现 |

### 4.2 技术评估发现的 12 个必须修正问题

| # | 问题 | 影响等级 | 解决方案 |
|---|---|---|---|
| 1 | 没有登录与用户隔离 | **严重** — 用户可读取/删除他人笔记 | 实现 JWT 认证 + 所有数据归属用户 |
| 2 | 多截图分别调用单图导入 | **高** — 选多张图生成多篇笔记 | 多图先上传，再创建合并导入任务 |
| 3 | 字段定义不一致 | **中** — 关键词被当作要点 | 明确区分关键词/要点/关键链接/正文/原文 |
| 4 | 详情接口没返回原文 | **中** — 原文已存但无法展示 | 分开定义列表响应与完整详情响应 |
| 5 | OCR/AI 在 HTTP 请求内等待 | **高** — 超时、重试重复创建 | 持久化任务状态 + 后台 Worker + 幂等提交 |
| 6 | 以为录音接口能直接转写 | **中** — 功能无法实现 | getRecorderManager 录音 + 腾讯云 ASR 识别 |
| 7 | 将小程序卡片转发当图片分享 | **中** — 与发朋友圈不是一回事 | 图片分享接口 + 卡片转发独立能力 |
| 8 | 网络图片直接保存相册 | **低** — API 调用失败 | 先下载为本地临时文件 |
| 9 | 因"Canvas 兼容性差"选后端渲染 | **低** — 增加服务端成本 | 改用官方 Canvas 2D 前端渲染 |
| 10 | 只用 create_all 更新数据库 | **中** — 已有表不会增加字段 | 引入 Alembic 做版本迁移 |
| 11 | URL 抓取未限制目标地址 | **严重** — SSRF 风险 | 校验协议/目标 IP/重定向/下载大小 |
| 12 | 国际化最后才加 | **中** — 大量返工 | 从第一批页面开始使用词典和主题变量 |

### 4.3 i18n 修复阶段发现的问题

| 问题 | 发现方式 | 解决方案 |
|---|---|---|
| 所有页面硬编码中文字符串 | 逐页代码审查 | 10 个页面全部重写为 `{{t('key', lang)}}` |
| `t()` 函数不支持占位符插值 | 实现时发现 | 改 `selectedCount` 为 `'张截图已选'`，数字单独显示 |
| `deleting` key 在词典中缺失 | grep 交叉验证 | 补充 zh/en 双语定义 |
| SOURCE_TYPE_MAP 硬编码中文值 | 代码审查 | 改为 SOURCE_TYPE_KEYS（i18n key 查找表） |

---

## 五、Sonus 能力评估

### 5.1 核心优势

**1. 全栈端到端能力**

从调研到上线级代码，一个会话内完成完整交付链：
- 调研：20 次搜索 + 11 次文档读取 + curl 实测
- 设计：10 张 UI 图 + 完整架构文档
- 实现：62 个源文件、4400 行代码、26 个任务
- 质量：主动发现问题 → 评估 → 修复 → 验证

这不是"回答问题"，是"交付项目"。

**2. 主动发现问题的能力**

技术评估阶段，Sonus **主动**对已实现的代码做了全面静态检查，发现了 12 个必须修正的问题——包括 2 个严重安全问题（无用户隔离、SSRF 风险）。这些问题如果不被发现，上线后会导致数据泄露和安全漏洞。

关键：这些问题不是用户提出的，是模型自己发现并主动报告的。

**3. 实测验证而非纸上谈兵**

- 视频号 URL 可行性：不是"搜索后给结论"，而是 curl 实测真实链接，拿到 2.6KB 空壳页面的证据
- 后端服务：实际启动 FastAPI 并 curl 验证每个端点
- 微信 API：读取官方文档后纠正了多个错误假设（录音≠转写、web-view 限制等）

**4. 上下文管理与任务跟踪**

- 7.5 小时内 6 次上下文溢出，每次都能从 summary 恢复继续
- 26 个任务全程跟踪，依赖关系清晰
- 跨 session 保持一致的代码风格和架构决策

**5. 设计迭代响应速度**

用户三次否定/修改设计方向，每次都能快速理解意图并重新生成：
- "太复杂了" → 立刻简化
- "三 Tab + 具体功能" → 精确对齐需求
- 不再纠结被否定的方案，没有废话

### 5.2 局限性

| 局限 | 具体表现 |
|---|---|
| 无法真机测试 | 小程序代码无法在本地运行验证，只能静态检查 |
| 上下文窗口有限 | 7.5h 内溢出 6 次，长项目需要频繁恢复 |
| 设计稿不能直接变代码 | ImageGen 生成的是效果图，不能直接转为 WXML/WXSS |
| 部分微信能力判断需实测 | 如视频号可行性，初次判断有误（说拿不到 URL），实测后纠正 |

### 5.3 效率对比

| 维度 | Sonus 表现 | 传统方式估算 |
|---|---|---|
| 调研到可运行代码 | 7.5 小时 | 1-2 周（一人全栈） |
| 代码产出 | 4,400 行 / 62 文件 | — |
| 工具调用 | 533 次 | — |
| 发现安全问题 | 2 个严重 + 2 个高危 | 可能需要安全审计才能发现 |
| 设计迭代 | 3 轮 / 10 张图 | 需要设计师介入 |

---

## 六、值不值得长期用

### 适合的场景

- **从零到一的项目**：调研 → 设计 → 实现一条龙，效率提升最明显
- **全栈开发**：前后端 + 部署 + 安全，不需要切换多个工具
- **快速原型**：7.5 小时从需求到可运行代码
- **技术调研**：搜索 + 实测 + 结论，比自己搜效率高

### 不太适合的场景

- 需要真机测试的移动端开发（只能写代码，不能跑）
- 需要长期维护的大型项目（上下文窗口是硬限制）
- 需要视觉设计精确定稿的场景（ImageGen 出概念图可以，像素级不行）

### 结论

**值得长期用，但要理解它的定位**：

Sonus 不是一个"更好的搜索引擎"或"更快的代码补全"——它是一个**能独立交付项目的全栈工程师**。核心价值在于：

1. **减少上下文切换**：一个人完成调研→设计→实现→测试→修复的完整闭环
2. **主动发现问题**：不只是执行指令，会自己审查代码并报告问题
3. **速度**：7.5 小时完成了传统方式 1-2 周的工作量

局限性是真实的（无法真机测试、上下文窗口有限），但在适合的场景下，效率提升是数量级的。

---

*报告生成时间：2026-09-17*
*数据来源：会话 transcript（2,823 行 JSONL）*
