# 交接：官网那两页的模板名要跟着小程序改（2026-09-25 00:0x）

给官网这条线（`/Users/zlfmac/Documents/AgentsWatermark`）。小程序这条线刚按站长 09-24 深夜的话把一套模板改了名，官网的模板屏上印的是同一批名字，不改就两边各叫一套。

**站长的原话**："模板里面的'人像封面'改为'杂志封面'"。

**要改的两处**（各一行，都在第 134 行的那个 `<figcaption>`）：

- `/Users/zlfmac/Documents/AgentsWatermark/cloud/ai-image-worker-site/index.html:134` —— `人像封面` → `杂志封面`
- `/Users/zlfmac/Documents/AgentsWatermark/cloud/ai-image-worker-site/en/index.html:134` —— `Portrait cover` → `Magazine cover`（那行小字 `人像封面` 一起换）

**小程序侧已经改完的事实**（`miniprogram/utils/poster.js` 的 `TEMPLATES`，界面与官网都读这一份口径）：十套名字现在是 经典卡片 / 金句大字 / 撞色块 / 极简 / 波普分格 / 网点漫画 / 荧光渐变 / **杂志封面** / 纸间文艺 / 规格卡；英文 Classic Card / Big Quote / Color Block / Minimal / Pop Panels / Ben-Day Comic / Acid Gradient / **Magazine Cover** / Warm Paper / Spec Card。`id` 仍是 `cover`，没动，所以不存在数据迁移。

**同一天小程序侧还有一条会影响官网措辞的**：卡片图小样里那句占位标题从"把读过的东西存成能转发的笔记"换成"读过的东西，存成能转发的笔记"，没设形象时人像位上的占位字从"标题第一个字"换成品牌字「麦」。官网模板屏那十张是 CSS 重画的示意（屏底已写明"示意，不是导出成品"），所以这两条**不要求官网跟着改**，写在这里只是别让两边对不上时被当成 bug。

改完要重新上线才算数（官网是静态页 + 备份回滚那套流程，见你们 `CHANGELOG.md`）。
