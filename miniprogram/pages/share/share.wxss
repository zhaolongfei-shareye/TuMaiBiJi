/* 海报预览：画布位图 750 宽，1 位图像素 = 1rpx，所以高度由 wxml 按算出来的
   行内 rpx 给就是等比。原来写死 80vh，高度改成随内容之后预览会被压扁。
   容器左右各有 --sp-3 的内边距，这里用负 margin 铺满屏宽，预览才和存出去的图一致。 */
.share-canvas {
  width: 750rpx;
  height: 1200rpx;
  margin-left: calc(var(--sp-3) * -1);
  /* 底部固定条现在是"模板名一行 + 按钮一行"，预览让开的距离跟着加一档。 */
  margin-bottom: 300rpx;
  background: transparent;
}

.loading-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--bg-page);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  font-size: var(--fs-body);
  font-weight: 600;
  color: var(--text-secondary);
}

.actions-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 0 var(--sp-3) calc(var(--sp-3) + env(safe-area-inset-bottom));
  background: var(--bg-page);
}

/* 只是告知"这张用的是哪套模板、去哪儿改"，不做成入口——同一件事只留一个地方能改。 */
.tpl-caption {
  padding: var(--sp-2) 0 var(--sp-1);
  font-size: var(--fs-meta);
  color: var(--text-tertiary);
  text-align: center;
}

.actions-row {
  display: flex;
  gap: var(--sp-2);
}

.actions-row .btn-secondary,
.actions-row .btn-primary {
  flex: 1;
  text-align: center;
  padding: var(--sp-3) 0;
  font-size: var(--fs-body);
}
