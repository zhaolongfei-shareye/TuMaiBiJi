.me-header {
  display: flex;
  align-items: center;
  gap: var(--sp-4);
  padding: var(--sp-4);
  margin-top: var(--sp-5);
}

.brand-logo {
  flex: none;
  width: 128rpx;
  height: 128rpx;
  border-radius: 40rpx;
  /* 原来这里垫了一层 --chip-idle 当底，因为旧 logo 自己不带底色。
     新 logo 是透明底圆标，再垫就会在四个角露出一块圆角方板。 */
}

.brand-text {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
  min-width: 0;
}

.brand-name {
  font-size: var(--fs-h2);
  font-weight: 800;
  letter-spacing: -0.8rpx;
  color: var(--text-primary);
}

.brand-slogan {
  font-size: var(--fs-meta);
  line-height: var(--lh-tight);
  color: var(--text-secondary);
}

/* ---------- 分享行 ----------
   入口退化成菜单里的一行：这一行本身就是 button[open-type=share]，
   所以要把小程序 button 的那堆默认样式（灰底、圆角、::after 描边、居中、行高）全部推平，
   让它和下面四行列表项长得一模一样——一个按钮态混进来就把节奏打断了。
   选择器写成 .menu-item.share-row：两条规则同为单类时后写的赢，
   .menu-item 在这个文件里更靠后，不加类名组合就会被它的 padding 盖掉。 */
.menu-item.share-row {
  margin: 0;
  /* 上下 24rpx：色块 52rpx 比一行字高，用别的行的 30rpx 会把整行顶高 7px */
  padding: 24rpx 0;
  width: 100%;
  box-sizing: border-box;
  border: none;
  border-bottom: var(--w-edge) solid var(--card-edge);
  border-radius: 0;
  background: transparent;
  color: var(--text-primary);
  font-size: var(--fs-body);
  font-weight: 700;
  line-height: var(--lh-tight);
  text-align: left;
  letter-spacing: -0.3rpx;
  white-space: normal;
}

.menu-item.share-row::after {
  border: none;
}

.menu-item.share-row:active {
  background: var(--accent-soft);
}

.menu-left {
  display: flex;
  align-items: center;
  gap: var(--sp-3);
  min-width: 0;
}

/* 52rpx 的小色块里只放一个汉字（项目没有 iconfont，单个汉字在苹方下必定出得来）。
   .blk 基础样式是"上下两端对齐"（给行卡的分类名 + 日期用的），这里只有一个字，
   所以要改成居中，否则字会顶在色块上沿。 */
.share-row .blk.dot {
  align-items: center;
  justify-content: center;
}

.dot-name {
  font-size: 26rpx;
  font-weight: 800;
  line-height: 1;
}

.menu-group {
  padding: 0 var(--sp-4);
  overflow: hidden;
}

/* 分组内的分隔线和卡片外描边同色同粗，不然一行里会出现两种"线" */
.menu-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 30rpx 0;
  border-bottom: var(--w-edge) solid var(--card-edge);
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-label {
  font-size: var(--fs-body);
  font-weight: 700;
  letter-spacing: -0.3rpx;
  color: var(--text-primary);
}

.menu-value {
  font-size: var(--fs-meta);
  color: var(--text-tertiary);
  margin-right: var(--sp-1);
}

.menu-right {
  display: flex;
  align-items: center;
}

.menu-arrow {
  font-size: 40rpx;
  font-weight: 300;
  line-height: 1;
  color: var(--text-tertiary);
}

.container {
  padding-bottom: 140rpx;
}
